# Frontend Production Environment Variables
#
# API calls use /api which Vercel proxies to https://auragram.onrender.com/api
# via vercel.json rewrites — no VITE_API_URL needed for REST.
#
# Socket.io connects directly (Vercel can't proxy WebSocket upgrades):
VITE_SOCKET_URL=https://auragram.onrender.com
